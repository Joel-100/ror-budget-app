<section>
  <div id="groups">
    <% @groups.each do |group| %>
      <div>
        <div class="card">
          <div class="card-body">
            <div class="image">
              <img src="<%= group.icon %>" alt="<%= group.name %>" />
            </div>
            <div>
              <h4 class="title"><%= group.name %></h4>
              <p>Total Amount: ₵<%= group.total_amount %></p>
              <p>
                <%= link_to "Transactions", user_group_deals_path(current_user, group) %>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
</section>
